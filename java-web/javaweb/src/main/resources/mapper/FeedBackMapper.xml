<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="niuniu.javaweb.mapper.FeedBackMapper">
    <insert id="insertFeedBack">
        insert into feedback (message, user_id, time, state)
        values (#{message}, #{userId}, current_timestamp, 0)
    </insert>
    <select id="getFeedBack" resultType="niuniu.javaweb.vo.FeedBackVO">
        select a.feedbackId, a.message, a.user_id, a.time, a.state, b.name
        from feedback as a,
             user as b
        where a.user_id = b.user_id
            ${ew.customSqlSegment}
    </select>
    <update id="checkFeedBack">
        update feedback
        set state = 1
        where feedback_id = #{feedbackId}
    </update>
</mapper>
